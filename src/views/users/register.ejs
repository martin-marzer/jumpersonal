<!DOCTYPE html>
<html lang="en">
<head>
    <title>Register</title>
    <%- include("./partials/head") %>
    <link rel="stylesheet" href="/css/register-login.css">
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous"> -->
</head>
<body>
    <%- include("./partials/header") %>
    <%- include("./partials/menuMobile") %>
    <main>
        <div class="img-form">
            <img src="images/contenido-pagina/sneakers-fondo-form.png" alt="fondo formulario">
        </div>
        <div class="container">
            <div id="jumpstore-form">
                <form class="formulario" action="/register" method="POST">
                    <div class="txt-title-content">
                        <h1>Crear cuenta</h2>
                        <h2>Creá tu perfil de miembro de Jumpstore y obtené acceso anticipado a los mejores productos, inspiración y comunidad Jumpstore.</h2>
                    </div>

                    <div class="field username">
                        <label for="username">Nombre de usuario:</label>
                        <input type="text"
                        name="username"
                        id="username"
                        class="form-control <%= locals.errors && errors.username ? 'is-invalid' : null %>"
                        value="<%= locals.oldData ? oldData.username : null %>"
                        >
                        
                        <% if (locals.errors && errors.username) { %>
                            <i class="fas fa-exclamation-circle"></i>
                            <div class="text-danger">
                                <%= errors.username.msg %> 
                            </div>
                        <% } %>
                    </div>

                    <div class="field email">
                        <label for="Email">Correo Electronico:</label>
                        
                        <input type="email" 
                        name="email" 
                        id="email"
                        class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %>"
                        value="<%= locals.oldData ? oldData.email : null %>"
                        >
                        <% if (locals.errors && errors.email) { %>
                            <i class="fas fa-exclamation-circle"></i>
                            <div class="text-danger">
                                <%= errors.email.msg %> 
                            </div>
                        <% } %>
                    </div>
                    <div class="field psw">
                        <label for="password">Contraseña:</label> 
                        <input type="password"
                        name="password"
                        id="password"
                        class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null %>"
                        value="<%= locals.oldData ? oldData.password : null %>" 
                        > 

                        <% if (locals.errors && errors.password) { %>
                            <i class="fas fa-eye eyePsw" style="bottom: 31px; right: 34px;"></i>
                            <i class="fas fa-exclamation-circle"></i>
                            <div class="text-danger">
                                <%= errors.password.msg %> 
                            </div>
                        <% } else {%>
                            <i class="fas fa-eye eyePsw"></i>
                        <% } %>
                         
                    </div>
                    <div class="field terminos">
                        <% if (locals.errors && errors.terminos) { %>
                            <input type="checkbox" name="terminos" id="terminos" >
                            <label for="terminos" style="color: #dc3545;">Estoy de acuerdo con los <a href="/terminos-y-condiciones">términos y condiciones</a></label>
                        <% } else {%>
                            <input type="checkbox" name="terminos" id="terminos">
                            <label for="terminos">Estoy de acuerdo con los <a href="/terminos-y-condiciones">términos y condiciones</a></label>
                        <% } %>
                    </div>
                    
                    <div class="field-cancelar-registrar">
                        <button type="button" id="delete-button">Cancelar</button>
                        <button type="submit">
                            Registrarse
                        </button>
                    </div>
                    <div class="miembro">
                        ¿Ya sos miembro?
                        <a href="/login">Inicia Sesión</a>
                    </div>
                   
                </form>
            </div>    
        </div>
    </main>
    <%- include("./partials/footer") %>
    <%- include("./partials/scripts") %>
    <script src="/js/register-login.js"></script>
</body>
</html>