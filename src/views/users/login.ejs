<!DOCTYPE html>
<html lang="en">
<head>
    <title>Login</title>
    <%- include("./partials/head") %>
    <link rel="stylesheet" href="/css/register-login.css">
</head>
<body>
    <%- include("./partials/header") %>

    <%- include("./partials/menuMobile") %>
    <main>
        <div class="img-form">
            <img src="images/contenido-pagina/sneakers-fondo-form.png" alt="fondo formulario">
        </div>
        <div class="container">
            <div id="jumpstore-form">
                <form class="formulario" action="/login" method="POST">
                    <div class="txt-title-content">
                        <h1>Entrar</h2>
                        <h2>¡Bienvenido de nuevo! Inicié sesión en su cuenta de Jumpstore.</h2>
                        <% if (locals.errorsGeneral) { %>
                            <div class="text-danger">
                                <%= errorsGeneral.msg %> 
                            </div>
                           <% } %>
                    </div>

                    <div class="field email" >
                        <label for="Email">Correo Electronico:</label>
                        <input type="email"
                        id="email"
                        name="email"
                        class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %>"
                        value="<%= locals.oldData ? oldData.email : null %>"
                        >
                        <% if (locals.errors && errors.email) { %>
                            <i class="fas fa-exclamation-circle" ></i>
                            <div class="text-danger">
                                <%= errors.email.msg %> 
                            </div>
                        <% } %>

                    </div>
                    <div class="field psw">
                        <label for="password">Contraseña:</label>
                        <input type="password"
                        name="password"
                        id="password"
                        class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %>"
                        value="<%= locals.oldData ? oldData.password : null %>" 
                        >
                        <% if (locals.errors && errors.password) { %>
                            <% if (errors.password.msg == "La contraseña debe contener una mayúscula, minúscula, número y un caracter especial") { %>
                                <i class="fas fa-eye eyePsw" style="right: 34px; bottom: 68px;"></i>
                                <i class="fas fa-exclamation-circle" style="bottom: 68px;"></i>
                            <% } else { %>
                                <i class="fas fa-eye eyePsw" style="right: 34px; bottom: 32px;"></i>
                                <i class="fas fa-exclamation-circle"></i>
                            <% } %>
                            <div class="text-danger">
                                <%= errors.password.msg %> 
                            </div>
                        <% } else {%>
                            <i class="fas fa-eye eyePsw"></i>
                        <% } %>
                    </div>
                    
                    <div class="field terminos">
                        <input type="checkbox" name="recordame" id="recordame">
                        <label for="recordame">Recordar mi usuario</label>
                    </div>
                    <div class="field-cancelar-registrar">
                        <button type="reset" id="delete-button">Cancelar</button>
                        <button type="submit">
                            Iniciar sesión
                        </button>
                    </div>
                    <div class="miembro">
                        ¿No tienes cuenta?
                        <a href="/register">Registrarse</a>
                    </div>
                   
                </form>
            </div>    
        </div>
    </main>
    <%- include("./partials/footer") %>
    <%- include("./partials/scripts") %>
    <script src="/js/register-login.js"></script>
</body>
</html>